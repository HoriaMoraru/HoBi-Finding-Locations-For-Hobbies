// src/pages/ExplorePage/types.ts

export interface LatLng {
    lat: number;
    lng: number;
  }

export  interface MyCustomRouteData {
    overviewPolyline: {
      encodedPath: string;
    };
  }
